<h1>{{ project.team.name }} / {{ project.name }}</h1>
<form>
<input name="q" value="{{ query }}" placeholder="Search" />
</form>
<ul>
{% for event in events %}
    <li>{{ event.object }}</li>
{% endfor %}
</ul>
